services:
  app:
    build:
      context: ../../..
      dockerfile: server/docker/registry/Dockerfile
    ports:
    - "8083:80"
    volumes:
      - ./input:/input
      - ./storage:/storage
    environment:
      STORAGE_PERSISTENCY: True