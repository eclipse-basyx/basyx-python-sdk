name: basyx-python-server
services:
  app:
    build:
      context: ../..
      additional_contexts:
        - sdk=../../../sdk
      dockerfile: ./docker/repository/Dockerfile
    ports:
    - "8080:80"
    volumes:
      - ./input:/input
      - ./storage:/storage
    environment:
      STORAGE_PERSISTENCY: True
